The frontend of the Rocket Control Panel is built with ReactJs and is located in the frontend directory.

Navigate and open to the Frontend Directory:

cd frontend -> cd rocketpanel -> npm run dev

---------------------------------------------

The backend is built with Node.js and Express.js and is located in the backend directory.

Navigate and open to the Backend Directory:

cd backend -> npm start 